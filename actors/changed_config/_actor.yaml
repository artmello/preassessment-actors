---
tags:
  - preupgradeassistant
inputs:
  - name: rpm_va
    type:
      name: RPMVAPackages
outputs:
  - name: changed_config
    type:
      name: ChangedConfig
description: |
  An actor to list all changed config files
execute:
  executable: /bin/bash
  script-file: changed_config.sh
