---
tags:
  - preupgradeassistant
outputs:
  - name: getent_passwd
    type:
      name: GetentPasswd
description: |
  An actor to list all users
execute:
  executable: /bin/bash
  script-file: getent_passwd.sh
